<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoApp">
        <activity
            android:name=".intro.SplashScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".intro.IntroActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp"/>

        <activity
            android:name=".login_regester.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp"/>

        <activity
            android:name=".login_regester.RegistrationActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp"/>

        <activity
            android:name=".login_regester.ForgetPasswordActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp"/>

        <activity
            android:name=".login_regester.ResetPasswordActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp"/>

        <activity
            android:name=".login_regester.SuccesfulActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoApp"/>

    </application>

</manifest>